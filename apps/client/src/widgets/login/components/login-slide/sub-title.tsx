import * as styles from './sub-title.css';

const SubTitle = () => {
  return (
    <p className={styles.descriptionText}>
      3분 설문으로 딱 맞는 보험을 추천해요!
      <br /> 쉽게 읽히는 리포트로 직접 판단할 수 있어요.
    </p>
  );
};

export default SubTitle;
